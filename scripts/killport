#!/bin/bash

if [ -z "$1" ]; then
  echo "Usage: killport <port>"
  exit 1
fi

PID=$(lsof -ti :$1)

if [ -z "$PID" ]; then
  echo "No process found on port $1"
  exit 0
fi

echo "Killing process $PID on port $1"
kill -9 $PID
